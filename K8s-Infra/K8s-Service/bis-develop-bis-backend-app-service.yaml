apiVersion: v1
kind: Service
metadata:
  annotations:
    # service.beta.kubernetes.io/azure-load-balancer-internal: "true"
    service.beta.kubernetes.io/azure-load-balancer-internal-subnet: "temp-dev-publicsubnet1"
  labels:
    io.kompose.service: temp-backend-app
  name: temp-backend-app
spec:
  type: LoadBalancer
    # loadBalancerIP: 10.240.0.25
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    io.kompose.service: temp-backend-app