apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
    # service.beta.kubernetes.io/azure-load-balancer-internal-subnet: "bis-dev-publicsubnet1"
  labels:
    io.kompose.service: bis-frontend-app
  name: bis-frontend-app
spec:
  type: LoadBalancer
    # loadBalancerIP: 10.240.0.25
  ports:
  - name: http
    port: 3456
    targetPort: 3456
  selector:
    io.kompose.service: bis-frontend-app