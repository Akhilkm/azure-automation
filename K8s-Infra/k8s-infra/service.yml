apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-internal: "true"
    # service.beta.kubernetes.io/azure-load-balancer-internal-subnet: "temp-dev-publicsubnet1"
  labels:
    io.kompose.service: temp-frontend-app
  name: temp-frontend-app
spec:
  type: LoadBalancer
    # loadBalancerIP: 10.240.0.25
  ports:
  - name: http
    port: 3456
    targetPort: 3456
  selector:
    io.kompose.service: temp-frontend-app